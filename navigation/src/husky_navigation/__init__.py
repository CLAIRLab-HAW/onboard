"""Navigation layer of the a200-0553.

ROS-FREE.  This package holds the decisions (driver configuration, topic and frame names, wiring), not the nodes -- so
that they can be checked on the Mac without ROS.  The launch files in ../launch/ import from here instead of stating
the values a second time.
"""

__version__ = "0.1.0"
