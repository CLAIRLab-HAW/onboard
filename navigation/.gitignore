__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.DS_Store
# ROS / colcon build
install/
log/
*.pcd
COLCON_IGNORE
AMENT_IGNORE
# a package run with --cov writes these; the workspace-wide measurement writes to the root instead
.coverage
.coverage.*
